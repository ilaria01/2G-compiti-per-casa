#include <iostream>
using namespace std;

int main ()
{ int file (0<=file>=0);
  void binary (file);
  if (valore=0)
    {
        cout << "0"
    }
  else
   {}
  if (valore=1)
    {
        cout << "0"
    }
  else
   {}
}
